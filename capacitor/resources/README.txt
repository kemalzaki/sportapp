# Letakkan dua file di sini sebelum menjalankan `npx capacitor-assets generate --android`:
#
#   icon.png   — 1024 × 1024  (logo penuh, latar boleh apa saja, akan dicrop)
#   splash.png — 2732 × 2732  (logo di tengah, sisanya warna #0f172a)
#
# Setelah itu jalankan:
#   cd capacitor
#   npx capacitor-assets generate --android
#   npx cap sync android
